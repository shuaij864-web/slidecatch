import { canonicalGenericResource,genericSessionKey,normalizeSessionPath,originPattern,safePageUrl,safeUrl,urlTokenScore } from '../core/url.js';
import { choosePageHint,extractPageNumberFromText,extractPageNumberFromUrl,pageHint } from '../core/page-number.js';
export const genericProvider=Object.freeze({id:'generic',name:'Generic web page',matchesLocation(){return true;},permissionPlan(rawUrl){const p=originPattern(rawUrl);return{pagePatterns:p?[p]:[],resourcePatterns:p?[p]:[]};},deriveSession({url,title=''}){const parsed=safeUrl(url),normalizedPath=parsed?normalizeSessionPath(parsed.pathname):'/';return{sessionKey:genericSessionKey(url),providerId:'generic',title:String(title||parsed?.hostname||'Web slides').trim().slice(0,200),pageUrl:safePageUrl(url),origin:parsed?.origin||'',stablePath:normalizedPath};},scoreCandidate(candidate){return urlTokenScore(candidate.url).score;},canonicalizeResource(rawUrl){return canonicalGenericResource(rawUrl);},extractPageHint({url,text='',attributes=[]}){const attributeText=attributes.filter(Boolean).join(' ');return choosePageHint(pageHint(extractPageNumberFromText(attributeText),95,'attribute'),pageHint(extractPageNumberFromText(text),85,'nearby-text'),pageHint(extractPageNumberFromUrl(url),65,'url'));},isStrongResource(){return false;}});
